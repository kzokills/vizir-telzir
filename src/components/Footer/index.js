import React, { Component } from "react";

const styles = {
  textAlign: "center"
}
class Footer extends Component {
  render() {
      return (
        <footer style={styles}>Esse é um projeto protótipo feito como forma de avaliação pela Vizir
        </footer>
    );
  }
}

export default Footer;